---
layout: layouts/base.njk
bodyClass: body-post
---
<article class="post" data-pagefind-body>
  <div class="post-header">
    <h1 class="post-title">{{ title }}</h1>

    <nav class="post-tags">
    {% for tag in tags %}
      <a href="/tags/{{ tag }}/" class="tag">{{ tag }}</a>
    {% endfor %}
    </nav>

    <time class="post-date" datetime="{{ date | date('DATETIME') }}">
      投稿日：{{ date | date('DATE') }}
    </time>
    {%- if updated %}
    <time class="post-date" datetime="{{ updated | date('DATETIME') }}">
      , 更新日：{{ updated | date('DATE') }}
    </time>
    {% endif %}
  </div>

  <div class="post-body">
    {{ content | safe }}
  </div>
</article>

<hr>

<nav class="post-navigation">
  <ul>
    {%- set previousPost = search.previousPage(url, "type=posts") %}
    {%- if previousPost %}
    <li>
      ← 前へ: <a href="{{ previousPost.data.url }}" rel="prev">{{ previousPost.data.title }}</a>
    </li>
    {% endif %}

    {%- set nextPost = search.nextPage(url, "type=posts") %}
    {%- if nextPost %}
    <li>
      <strong>次へ: <a href="{{ nextPost.data.url }}" rel="next">{{ nextPost.data.title }}</a> →</strong>
    </li>
    {% endif %}
  </ul>
</nav>